# prompts

Prompt templates used to generate structured synthetic samples. Each template
encodes formatting rules, constraints, and examples for a specific signal
family. The pipeline loads these templates, fills in dynamic fields, and sends
them to the LLM backend during sampling.
