# slm_synth

Core implementation of the synthetic‑data pipeline. This module handles prompt
loading, LLM sampling, schema validation, deduplication, and dataset export.
All pipeline logic lives here, organized into a single cohesive module.
